---
description: Autonomous job fetching, scoring, and wisdom evolution (run after /setup and profile review)
globs: README.md, data/candidate_info.json, data/*resume*.pdf
---

# /fetchjobs

RULE_HIT_CHECK: jobsearch.mdc triggered for /fetchjobs

Read `.claude/commands/fetchjobs.md` and execute that procedure exactly.

**Cursor delta (step 4 only):** `persist_jobs` also appends snapshots to `data/history/candidate_history.db` and `data/history/intelligence_history.db` in addition to `jobs_history.db`.
